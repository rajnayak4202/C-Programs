#include<stdio.h>
#include<conio.h>
void main()
{
	int ano[50]={0},no,a,b,ans,count=0,i;
	clrscr();
	printf("enter=");
	scanf("%d",&no);
	for(a=1;a<=no;a++)
	{
		for(b=2;b<a;b++)
		{
			ans=a%b;
			if(ans==0)
			{
				break;
			}
		}
		if(ans!=0)
		{
			ano[count]=a;
			count++;
		}
	}
	for(i=0;i<count;i++)
	{
		printf("[%d]-",ano[i]);
	}
	getch();
}